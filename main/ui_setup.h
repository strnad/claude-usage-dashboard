/*
 * Setup-mode screen — shown when device runs in AP mode (captive portal active).
 *
 * Displays the AP SSID and the URL to open after connecting.
 */

#pragma once

void ui_setup_show(const char *ap_ssid);
